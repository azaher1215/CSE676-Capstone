# CSE676-Capstone

## One-command reproduction:

- Open the notebook in Google Colab (GPU runtime), set a DEEPINFRA_KEY secret, and run all cells.
- This line run_all(N=150, N_ablation=100) will regenerate summary.csv and predictions.jsonl but expect it to take a few hours.
